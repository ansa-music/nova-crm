export function GlobalMessageToaster() {
  // Inbox is no longer watched from AppLayout (Spark read budget).
  // Unread badges poll on the sidebar; full threads load on /messages and /chat.
  return null;
}
